<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Capitec Business Tracker</title>
  <link href="https://cdn.jsdelivr.net/npm/@tailwindcss/typography@0.5.9/dist/typography.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/daisyui@3.9.4/dist/full.css" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    :root {
      --fallback-s: 72.2%;
      --fallback-sf: 72.2%;
      --s: 72.2%;
      --sf: 72.2%;
      --fallback-sc: 0.743;
      --sc: 0.743;
      --fallback-sfc: 0.743;
      --sfc: 0.743;
      --primary: 200 100% 21%; /* #00486d - dark blue */
      --secondary: 7 81% 57%; /* #e63934 - red */
    }
  </style>
</head>
<body>
  <div class="min-h-screen bg-white">
    <!-- Hero Section -->
    <div class="hero min-h-screen">
      <div class="hero-content text-center">
        <div class="max-w-md">
          <div class="mb-8">
            <h1 class="text-5xl font-bold text-primary">Capitec</h1>
            <h2 class="text-3xl font-semibold text-secondary">Invoice Tracker</h2>
          </div>
                    
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <% if (typeof user !== 'undefined' && user) { %>

            <% } else { %>
              <!-- Guest user buttons -->
              <a href="/login" class="btn btn-primary btn-lg">
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"></path>
                </svg>
                Sign In
              </a>
              <a href="/register" class="btn btn-secondary btn-lg btn-outline">
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path>
                </svg>
                Create Account
              </a>
            <% } %>
          </div>
          
          <% if (typeof user !== 'undefined' && user) { %>
            <!-- Welcome message for authenticated users -->
            <div class="mt-8 alert alert-info">
              <svg class="stroke-current shrink-0 w-6 h-6" fill="none" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              <span>Welcome back, <strong><%= user.username %></strong>!</span>
            </div>
            
            <!-- Navigation options for authenticated users -->
            <div class="mt-8 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
              <a href="/invoices" class="card bg-base-100 shadow-lg hover:shadow-xl transition-shadow">
                <div class="card-body text-center">
                  <svg class="w-8 h-8 mx-auto mb-2 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                  </svg>
                  <h3 class="card-title text-sm">Invoices</h3>
                  <p class="text-xs text-gray-500">View all invoices</p>
                </div>
              </a>
              
              <form action="/logout" method="POST" class="inline">
                <button type="submit" class="card bg-base-100 shadow-lg hover:shadow-xl transition-shadow w-full h-full">
                  <div class="card-body text-center">
                    <svg class="w-8 h-8 mx-auto mb-2 text-secondary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                    </svg>
                    <h3 class="card-title text-sm">Logout</h3>
                    <p class="text-xs text-gray-500">Sign out</p>
                  </div>
                </button>
              </form>
            </div>
          <% } %>
        </div>
      </div>
    </div>
  </div>
</body>
</html>